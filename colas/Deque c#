using System;
using System.Collections.Generic;

class Program {
    static void Main() {
        LinkedList<int> d = new LinkedList<int>();
        d.AddFirst(10);
        d.AddLast(20);
        d.AddFirst(5);

        foreach (int x in d)
            Console.Write(x + " ");
    }
}
