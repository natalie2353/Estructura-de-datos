class Cola {
    int[] arr = new int[5];
    int front = -1, rear = -1;

    void enqueue(int x) {
        if (rear == 4) return;
        if (front == -1) front = 0;
        arr[++rear] = x;
    }

    void dequeue() {
        if (front == -1 || front > rear) return;
        front++;
    }
}
